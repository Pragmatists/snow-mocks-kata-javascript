function MunicipalService() {
    return {
        sendSander: function () {
            // we don't care what the implementation of this method is
        },
        sendSnowplow: function () {
            // we don't care what the implementation of this method is
        }
    }
}

if (typeof exports !== 'undefined') {
    module.exports = MunicipalService;
}